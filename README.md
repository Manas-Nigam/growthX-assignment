# GrowthX Assignment — React Animation Demo

Live demo: run locally (instructions below).

## Features

- "ELEVATE" entrance animation with coordinated elements:
  - Sparkle and scoop shapes in the header area.
  - Fade and translate transitions applied to the container and text.
- A floating animated button (in the app root) to re-trigger the effect via a page reload.
- SCSS variables for quick tuning of colors, spacing and durations (`src/App.scss`).

## How to run

1. Install dependencies:
   ```sh
   npm install
